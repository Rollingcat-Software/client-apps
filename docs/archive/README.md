# Documentation Archive

Historical docs preserved for git history. Content here is **not current** — see top-level `README.md`, `CHANGELOG.md`, `ROADMAP_CLIENT_APPS.md`, and `docs/` for authoritative docs.

## 2026-04-16

Bulk cleanup: moved 13 root-level drafts (quickstart duplicates, camera/WebAuthn implementation notes, early architecture drafts) out of the repo root. Each was superseded by current code comments, the top-level README, or `docs/DEPLOYMENT_CHECKLIST.md` / `docs/PERFORMANCE.md`.
